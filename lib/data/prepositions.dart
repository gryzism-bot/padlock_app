import 'package:padlock_app/models/language.dart';

import '../models/grammar/preposition.dart';

const at = Preposition('at', {Language.pl: 'na'});
const inPreposition = Preposition('in', {Language.pl: 'w'});
const on = Preposition('on', {Language.pl: 'na'});
const under = Preposition('under', {Language.pl: 'pod'});
const over = Preposition('over', {Language.pl: 'nad'});
const behind = Preposition('behind', {Language.pl: 'za'});
const inFrontOf = Preposition('in front of', {Language.pl: 'przodem'});
const near = Preposition('near', {Language.pl: 'blisko'});
const beside = Preposition('beside', {Language.pl: 'obok'});
const nextTo = Preposition('next to', {Language.pl: 'obok'});
const between = Preposition('between', {Language.pl: 'pomiędzy'});
const by = Preposition('by', {Language.pl: 'przez'});
const inside = Preposition('inside', {Language.pl: 'wewnątrz'});
const outside = Preposition('outside', {Language.pl: 'na zewnątrz'});
const to = Preposition('to', {Language.pl: 'do'});
const from = Preposition('from', {Language.pl: 'z'});
const into = Preposition('into', {Language.pl: 'do'});
const onto = Preposition('onto', {Language.pl: 'na'});
const outOf = Preposition('out of', {Language.pl: 'z'});
const off = Preposition('off', {Language.pl: 'z'});
const through = Preposition('through', {Language.pl: 'przez'});
const across = Preposition('across', {Language.pl: 'przez'});
const along = Preposition('along', {Language.pl: 'wzdłuż'});
const around = Preposition('around', {Language.pl: 'wokół'});
